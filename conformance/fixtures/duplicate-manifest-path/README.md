# Invalid Fixture: Duplicate Manifest Path

Expected result:

- validator fails publication validation;
- validator reports `SSP_MANIFEST_MISMATCH`;
- `.ssp/manifest.json` contains the same step path twice.

