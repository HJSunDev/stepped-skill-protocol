# Invalid Fixture: Entry Path Traversal

Expected result:

- validator fails publication validation;
- validator reports `SSP_ENTRY_MISSING`;
- `stepped-skill.entry` attempts to escape the Skill package.

