# Invalid Fixture: Frontmatter Resource Mismatch

Expected result:

- validator fails publication validation;
- validator reports `SSP_MANIFEST_MISMATCH`;
- generated step frontmatter declares resources that body `Resources` does not declare.

