# missing-frontmatter

Invalid fixture for base Agent Skills compatibility.

Expected result:

- validator reports `SSP_AGENT_SKILL_INVALID`;
- package fails because `SKILL.md` does not start with YAML frontmatter.

