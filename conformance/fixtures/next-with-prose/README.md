# next-with-prose

Invalid fixture for step transition syntax.

Expected result:

- validator reports `SSP_NEXT_INVALID`;
- package fails because `Next` contains explanatory prose instead of exactly one transition target.

