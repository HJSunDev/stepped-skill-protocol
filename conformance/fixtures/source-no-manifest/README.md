# Source Fixture: No Manifest

Expected result:

- source validation passes;
- publication validation fails with `SSP_PACKAGE_INVALID`;
- the package proves author-source checks do not require generated `.ssp/manifest.json`.

