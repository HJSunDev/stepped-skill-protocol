# invalid-metadata-shape

Invalid fixture for Agent Skills metadata compatibility.

Expected result:

- validator reports `SSP_AGENT_SKILL_INVALID`;
- package fails because metadata contains a non-string value.

