# Invalid Fixture: Next Directory

Expected result:

- validator fails publication validation;
- validator reports `SSP_NEXT_INVALID`;
- `Next` points at a directory-like path instead of a step file.

