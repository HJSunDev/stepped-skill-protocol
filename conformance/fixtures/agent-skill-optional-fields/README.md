# agent-skill-optional-fields

Valid fixture for Agent Skills optional field compatibility.

Expected result:

- source validation passes;
- publication validation passes;
- package remains SSP-valid while using `license`, `compatibility`, and `allowed-tools`.
