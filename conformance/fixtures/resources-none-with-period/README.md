# resources-none-with-period

Invalid fixture for SSP Resources syntax.

Expected result:

- validator reports `SSP_RESOURCE_UNREADABLE`;
- package fails because `Resources` uses `None.` instead of the exact value `None`.
