# invalid-license-scalar

Invalid fixture for Agent Skills compatibility.

Expected result:

- validator reports `SSP_AGENT_SKILL_INVALID`;
- package fails because optional `license` is an unquoted YAML boolean.
