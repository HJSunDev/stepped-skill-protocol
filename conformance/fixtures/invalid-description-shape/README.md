# invalid-description-shape

Invalid fixture for Agent Skills compatibility.

Expected result:

- validator reports `SSP_AGENT_SKILL_INVALID`;
- package fails because `description` is not a scalar string.

