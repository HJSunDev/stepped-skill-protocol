# empty-instructions

Invalid fixture for step content quality.

Expected result:

- validator reports `SSP_STEP_MISSING_SECTION`;
- package fails because a required step section exists but is empty.

