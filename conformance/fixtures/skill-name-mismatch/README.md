# skill-name-mismatch

Invalid fixture for Agent Skills compatibility.

Expected result:

- validator reports `SSP_AGENT_SKILL_INVALID`;
- package fails because `SKILL.md` name does not match the package directory.

